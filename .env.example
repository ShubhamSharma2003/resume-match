OPENAI_API_KEY=your_api_key_here
OPENAI_MODEL=gpt-5.4-mini
APP_PASSWORD=shubhamsharma
# Signs session tokens. Derived from APP_PASSWORD when unset; set it explicitly so
# tokens survive a password change.
AUTH_SECRET=
PORT=8787
